apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "honeyhive-api.fullname" . }}-test-connection"
  labels:
    {{- include "honeyhive-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "honeyhive-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
