apiVersion: v1
kind: Service
metadata:
  name: {{ include "honeyhive-api.fullname" . }}
  labels:
    {{- include "honeyhive-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "honeyhive-api.selectorLabels" . | nindent 4 }}
